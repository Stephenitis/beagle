<% @response["results"].each do |bill_hash| %>

<ul>
 	<li>
    <%= link_to(short_title_handler(bill_hash), bills_path(:bill_id => bill_hash["bill_id"]), :method => :post) %> </li>
  <li>
  	<%= official_title_handler(bill_hash['official_title']) %>
  </li>
</ul>

<% end  %>
